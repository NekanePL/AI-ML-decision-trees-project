import pandas an pd
import nunpy as np
from _future_ import print_function
training_data = [
    ['bird', 'big', 'wild', 'yes']
    ['bird', 'small', 'both', 'no']
    ['mamal', 'big', 'both', 'no']
    ['mamal', 'medium', 'domestic', 'yes']
    ['mamal', 'small', 'both', 'maybe']
    ['mamal', 'small', 'domestic', 'no']
    ['repitle', 'small', 'both', 'mo']
    ['mamal', 'big', 'wild', 'yes']
    ['mamal', 'medium', 'domestic', 'maybe']
    ['reptile', 'medium', 'both', 'yes']
    ['reptile', 'small', 'wild', 'no']
header = ["Type", "Size", "Domestic or Wild", "Carnivor or other"]
def unique_vals(rows, col):
    return set(row[col] for row in rows])
unique_vals(training_data,0)
def class_counts(rows):
    for row in rows:
        label = row[-1]
        if label not in counts:
              counts[label] = 0
        counts[label] += 1
    return counts
class_counts(training_data)

q = Question(0, 'Mamal')
def build_tree(rows):
